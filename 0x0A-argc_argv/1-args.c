#include "main.h"

/**
* main: Display the number of arguments
* @argc: the number of arguments
* @argv: An array of argument
* Return: number of argument
*/

int main(int argc, char *argv[])
{
printf("%d\n", argc - 1);
return (0);
}
