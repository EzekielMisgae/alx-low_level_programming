#include "main.h"

/**
* main: Display the number
* @argc: The number of arg supplied to the program
* @argv: An array of pointers to the argument
* @n: argument value of except 0
* Return: Always number
*/

int main(int argc, char* argv[])
{
printf("%d\n", argc - 1);
return (0);
}
